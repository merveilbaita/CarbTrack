Django>=5.0,<5.2
djangorestframework>=3.15
psycopg[binary]>=3.1
python-dotenv>=1.0
gunicorn>=22.0
whitenoise>=6.7          # sert les fichiers statiques (admin) en prod
dj-database-url>=2.2     # parse DATABASE_URL (Neon)
# Phase 4 — temps réel (WebSockets)
channels>=4.1
channels-redis>=4.2
daphne>=4.1
